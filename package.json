{
  "name": "gif-scrubber",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox extension to play, pause, scrub, and frame-step standalone GIF tabs.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "package": "node scripts/package.mjs"
  },
  "engines": {
    "node": ">=20.10.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "fflate": "^0.8.2",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "gifuct-js": "^2.1.2"
  }
}
